# Task 1 — inference-only run of the uncertainty proxies on pretrained DiT-XL/2.
#
# Install torch FIRST, matched to the server's CUDA — do not let pip pick it:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121   # CUDA 12.1
#   pip install torch --index-url https://download.pytorch.org/whl/cu118   # CUDA 11.8
# then:
#   pip install -r requirements-server.txt

timm>=0.9              # DiT/models.py: PatchEmbed, Attention, Mlp
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
tqdm>=4.65

# --- only if you also want to DECODE latents into images for the slides ---
# The proxy pipeline is latent-space only and does not need these.
# diffusers>=0.27
# safetensors
# Pillow

# --- only if running as a notebook rather than scripts/ ---
# jupyterlab
# ipywidgets            # tqdm.auto progress bars in JupyterLab
